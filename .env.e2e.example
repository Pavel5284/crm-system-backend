# Локальный e2e-режим без Docker (файл НЕ коммитить).
# Тестовая БД и vhost должны существовать и быть ПУСТЫМИ/тестовыми —
# прогон чистит таблицы и гоняет миграции. НЕ указывать сюда прод!
E2E_DATABASE_URL="postgresql://crmsystem:CHANGE_ME@localhost:5433/taskmanager_test?schema=public"
E2E_RABBITMQ_URL="amqp://guest:guest@localhost:5672/e2e"
